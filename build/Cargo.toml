[package]
name = "treeldr-build"
version = "0.1.0"
edition = "2021"

[dependencies]
treeldr = { path = "../core" }
log = "0.4"
rdf-types = { version = "0.9", features = ["meta"] }
iref = "2.2"
shelves = "0.2.6"
derivative = "2.2.0"
locspan = { version = "0.7.2", features = ["reporting"] }
locspan-derive = "0.4"
codespan-reporting = "0.11"
grdf = { version = "0.11", features = ["meta"] }