use <https://schema.org/> as schema;
use <https://tzprofiles.com/> as tz;

type DiscordVerification {
	schema:url
}

type DiscordVerificationMessage {
	tz:handle,
	tz:timestamp,
	tz:DiscordChannelId as channelId,
	tz:DIscordMessageId as messageId
}